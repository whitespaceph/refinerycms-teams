<%= render 'refinery/admin/search_header', :url => refinery.teams_admin_teams_path %>
<div class='pagination_container'>
  <% if @teams.any? %>
    <%= render 'teams' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
